"use client";

import { ShoppingCart } from "@phosphor-icons/react";

export const AppBar = () => (
  <header className="flex items-center justify-between">
    <h1>trooth</h1>

    <nav></nav>

    <ShoppingCart />
  </header>
);
